# Klarbog UI assets

Application UI is **Rust Askama SSR** in `klarbog-api` (ADR-018).

This directory keeps **non-logic** static assets only (`styles.css`).
The former JS SPA (`app.js`, `js/*`, `index.html`) is retired.
