# Active swarm status

- slice: 0–1 done; slice 2 partial (ConfirmStore + pathguard + CRM plugin stub)
- verify: VERIFY_OK
- tip: see git log on rust-dev
- stop_file: **absent by default**. Create `swarm/STOP` to halt `scripts/swarm_dispatch.py`.
